import { Button } from '../components/button'

function Promo() {
    return (
        <main>
            <h1>Messenger</h1>
            <Button value='Get started' link='/signup' />
        </main>
    );
}

export default Promo;
